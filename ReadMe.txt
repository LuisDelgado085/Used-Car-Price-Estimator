To run this code you will need to install the python libraries numpy, pandas, scipy and scikit-learn. 

This project was done on jupyter notebook thus, using an IDE that can integrate jupyter notebook is needed.